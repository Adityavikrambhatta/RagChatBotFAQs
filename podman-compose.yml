services:
  backend:
    build:
      context: .
      dockerfile: Containerfile.backend
    container_name: ragchatbot-backend
    env_file:
      - .env
    environment:
      RAG_APP_DATA_DIR: /app/data
      RAG_APP_DEFAULT_CORPUS: document-qa
      RAG_APP_COLLECTION_PREFIX: rag_
      RAG_APP_CHUNK_SIZE: 1000
      RAG_APP_CHUNK_OVERLAP: 200
      RAG_APP_DEFAULT_TOP_K: 4
      RAG_APP_EMBEDDING_PROVIDER: openai
      RAG_APP_EMBEDDING_MODEL: text-embedding-3-small
      RAG_APP_LLM_PROVIDER: openai
      RAG_APP_OPENAI_MODEL: gpt-4.1-mini
      RAG_APP_LLM_TEMPERATURE: 0.0
      RAG_APP_FRONTEND_ORIGIN: http://localhost:8080
    ports:
      - "8000:8000"
    volumes:
      - rag-data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Containerfile
      args:
        VITE_API_BASE_URL: ""
    container_name: ragchatbot-frontend
    depends_on:
      - backend
    ports:
      - "8080:8080"
    restart: unless-stopped

volumes:
  rag-data:
